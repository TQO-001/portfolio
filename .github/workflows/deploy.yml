name: Deploy Portfolio

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 📋 Checkout Code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 🏗️ Install & Build
        run: |
          npm ci
          npm run build
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}

      - name: 🚚 Sync Files to VPS
        uses: easingthemes/ssh-deploy@main
        with:
          # These MUST be uppercase for this specific action
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          REMOTE_HOST: "laughtale.co.za"
          REMOTE_USER: "laughtale"
          REMOTE_PORT: ${{ secrets.PORT }}
          TARGET: "/var/www/portfolio"
          SOURCE: "./"
          ARGS: "-rlgoDzvc -i --delete --exclude='.git' --exclude='node_modules' --exclude='src'"

      - name: 🚀 Production Launch & Restart
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: "laughtale.co.za"
          username: "laughtale"
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            cd /var/www/portfolio
            
            # Use APP_PORT for the .env
            cat << EOF > .env
            RESEND_API_KEY="${{ secrets.RESEND_API_KEY }}"
            PORT=${{ secrets.APP_PORT }}
            NODE_ENV="production"
            EOF
            
            npm install --omit=dev --ignore-scripts
            
            # Use APP_PORT to tell Next.js where to listen
            pm2 delete portfolio || true
            pm2 start npm --name "portfolio" -- start -- -p ${{ secrets.APP_PORT }}
            pm2 save

      - name: ✅ Health Check
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: "laughtale.co.za"
          username: "laughtale"
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            sleep 10
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:${{ secrets.APP_PORT }} || echo "000")
            
            if [ "$STATUS" = "200" ] || [ "$STATUS" = "307" ] || [ "$STATUS" = "302" ]; then
              echo "✅ Portfolio is LIVE on port ${{ secrets.APP_PORT }} (HTTP $STATUS)"
            else
              echo "❌ Health check failed (HTTP $STATUS)"
              pm2 logs portfolio --lines 20 --nostream
              exit 1
            fi